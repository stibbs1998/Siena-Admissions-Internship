from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This is a project with the al of finding a "critical path" for admitting students to Siena College.',
    author='Spencer Tibbitts',
    license='',
)
